"use client";
export default function PayoutsView({ sessionId: _sessionId, userId: _userId, isHost: _isHost, sessionState: _sessionState }: { sessionId: string; userId: string; isHost: boolean; sessionState: string }) {
  return <p className="text-gray-500">Payouts — coming soon</p>;
}