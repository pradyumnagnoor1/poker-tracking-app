"use client";
export default function ChipCountView({ sessionId: _sessionId, userId: _userId, isHost: _isHost }: { sessionId: string; userId: string; isHost: boolean }) {
  return <p className="text-gray-500">Chip count phase — coming soon</p>;
}